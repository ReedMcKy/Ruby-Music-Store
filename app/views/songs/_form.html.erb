<%= form_with(model: song) do |form| %>
  <% if song.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
        <% song.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :song_title, style: "display: block" %>
    <%= form.text_field :song_title %>
  </div>

  <div>
    <%= form.label :album_title, style: "display: block" %>
    <%= form.text_field :album_title %>
  </div>

  <div>
    <%= form.label :artist_name, style: "display: block" %>
    <%= form.text_field :artist_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
